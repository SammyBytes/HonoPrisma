{
  "name": "hono-prisma",
  "version": "0.0.1",
  "description": "Prisma Middleware for Hono",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && bun build src/index.ts --outdir dist --target bun --external:hono --external:typescript --minify",
    "test": "bun test",
    "prepare": "bun run build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "hono",
    "prisma",
    "middleware",
    "typescript",
    "bun"
  ],
  "author": "Samuel Luciano",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5",
    "hono": "^4.9.6",
    "@prisma/client": "^5"
  },
  "dependencies": {
    "hono": "^4.9.6"
  }
}
