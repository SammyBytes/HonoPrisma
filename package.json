{
  "name": "hono-prisma-plugin",
  "version": "0.0.2",
  "description": "Prisma ORM plugin for HonoJS framework",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "require": "./src/index.ts",
      "types": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc && bun build src/index.ts --outdir dist --target bun --external:hono --external:@prisma/client --minify",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "prepare": "bun run build"
  },
  "files": [
    "src",
    "dist"
  ],
  "keywords": [
    "hono",
    "prisma",
    "middleware",
    "typescript",
    "bun"
  ],
  "author": "Samuel Luciano",
  "readme": "README.md",
  "repository": {
    "type": "git",
    "url": "git@github.com:SammyBytes/HonoPrisma.git"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "hono": ">=4.0.0",
    "@prisma/client": ">=4.0.0"
  },
  "dependencies": {}
}
